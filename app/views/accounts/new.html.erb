<%= simple_form_for :account, url: cooperative_accounts_path(@cooperative) do |f| %>
  <%= f.input :email%>
  <%= f.input :password  %>
  <%= f.input :role, as: :radio_buttons, collection: User.cooperative_roles %>
  <%= f.button :submit  %>
<% end %>          

