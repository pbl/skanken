<div class="page-header">
  <h1>Skånken <small>A better work list for Lunds nations</small></h1>
</div>
<div class="row">
  <div class="col-md-6">
    <div class="well well-lg">
      <h2>Log in</h2>

      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-inputs">
          <%= f.input :email, required: false, autofocus: true %>
          <%= f.input :password, required: false %>
          <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
        </div>
        <div class="form-actions">
          <%= f.button :submit, "Log in" %>
        </div>
      <% end %>
    </div>
  </div>

  <div class="col-md-6">
    <div class="well well-lg">
      <h2>Create a new list of workers</h2>
      <p>This will create a new cooperative where you can have a list of workers. </p>
      <p>If you wish to sign up for a already existing cooperative please contact the cooperative so they can create an account for you</p>
      <p><a class="btn btn-primary btn-lg" href=<%=new_registration_path(resource_name) %> role="button">Create new worker list</a></p>
    </div>
  </div>
</div>