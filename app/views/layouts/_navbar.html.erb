<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% @cooperative = Cooperative.find(current_user.cooperative_id) %>
        <li><%= link_to 'Worker list', cooperative_members_path(@cooperative) %></li>
        <li><%= link_to 'Add worker', new_cooperative_member_path(@cooperative) %></li>
        <li><%= link_to 'Creators', info_creators_path %></li>
        <li><%= link_to 'Sign out', destroy_user_session_path, :method=>'delete' %></li>
      </ul>
      <% if can? :manage, Cooperative %>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Admin<span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to 'Account list', cooperative_accounts_path(@cooperative) %></li>
            <li><%= link_to 'Add account', new_cooperative_account_path(@cooperative) %></li>
            <li><%= link_to 'Edit cooperative', edit_cooperative_path(@cooperative) %></li>
            <li><%= link_to 'Manage worker list', cooperative_admin_path(@cooperative) %></li>
          </ul>
        </li>
      </ul>
      <% end %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>