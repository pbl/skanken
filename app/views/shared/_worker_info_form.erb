<%= simple_form_for type do |f| %>
  <%= f.input :activity, label: "Activities", as: :radio_buttons, collection: @activities %>
  <%= f.input :comment, required: false, as: :text  %>
  <%= f.button :submit  %>
<% end %>