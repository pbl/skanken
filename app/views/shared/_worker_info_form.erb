<%= simple_form_for type do |f| %>
  <%= f.input :activity, label: "Activities", as: :radio_buttons, collection: Cooperative.activities(current_user.cooperative_id) %>
  <%= f.input :comment, required: false, as: :text  %>
  <%= f.button :submit  %>
<% end %>