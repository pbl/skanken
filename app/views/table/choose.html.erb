<h2> <%= t('table.choose.header') %></h2>
<% @activated.each do |activity| %>
  <%= link_to activity.name, table_create_path(choose: { activity_id: activity.id}), class: 'btn btn-default' %>
<% end %>

